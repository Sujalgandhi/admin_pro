<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Password</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #121212;
            /* Dark background color */
        }

        .bg-dark-gray {
            background-color: #1f2937;
            /* Darker background color */
        }

        .text-light-gray {
            color: #f9fafb;
            /* Light text color */
        }

        .text-gray {
            color: #d1d5db;
            /* Slightly lighter text color */
        }

        .bg-darker-gray {
            background-color: #111827;
            /* Dark background for input */
        }

        .border-gray {
            border-color: #4b5563;
            /* Dark border color */
        }

        .hover-bg-blue:hover {
            background-color: #2563eb;
            /* Darker blue on hover */
        }

        .focus-ring-blue:focus {
            box-shadow: 0 0 0 0.2rem rgba(38, 99, 235, 0.5);
            /* Custom ring color */
        }
    </style>
</head>

<body class="d-flex align-items-center justify-content-center min-vh-100 bg-dark">
    <form action="changepassword" method="post" class="bg-dark-gray p-4 rounded-lg shadow-lg w-100" style="max-width: 400px;">
        <h2 class="text-center mb-4 text-light-gray">Change Password</h2>
        <div class="form-group">
            <label for="oldpassword" class="text-gray">Old Password</label>
            <input type="password" name="oldpassword" id="oldpassword" placeholder="Enter old password"
                class="form-control bg-darker-gray text-light-gray border border-gray focus-ring-blue" required>
        </div>
        <div class="form-group">
            <label for="newpassword" class="text-gray">New Password</label>
            <input type="password" name="newpassword" id="newpassword" placeholder="Enter new password"
                class="form-control bg-darker-gray text-light-gray border border-gray focus-ring-blue" required>
        </div>
        <div class="form-group">
            <label for="confirmpassword" class="text-gray">Confirm Password</label>
            <input type="password" name="confirmpassword" id="confirmpassword" placeholder="Enter confirm password"
                class="form-control bg-darker-gray text-light-gray border border-gray focus-ring-blue" required>
        </div>
        <button type="submit" class="btn btn-primary w-100">Submit</button>
    </form>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
